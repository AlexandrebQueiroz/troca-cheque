import { CalculateProposalPipe } from './calculate-proposal.pipe';

describe('CalculateProposalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateProposalPipe();
    expect(pipe).toBeTruthy();
  });
});

describe('Must calculate 0 days cheque', () => {
  it('create an instance', () => {
    const pipe = new CalculateProposalPipe();
    expect(pipe).toBeTruthy();
  });
});

